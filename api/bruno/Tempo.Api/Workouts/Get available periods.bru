meta {
  name: Get available periods
  type: http
  seq: 18
}

get {
  url: {{baseUrl}}/stats/available-periods
  body: none
  auth: inherit
}

params:query {
  ~timezoneOffsetMinutes: 
}

example {
  name: 200 Response
  description: OK
  
  request: {
    url: {{baseUrl}}/stats/available-periods
    method: GET
    mode: none
    params:query: {
      ~timezoneOffsetMinutes: 
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
