meta {
  name: Bulk import Strava export
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/workouts/import/bulk
  body: multipartForm
  auth: inherit
}

example {
  name: 202 Response
  description: Accepted — poll GET /workouts/import/jobs/{id}
  
  request {
    url: {{baseUrl}}/workouts/import/bulk
    method: POST
    mode: multipartForm
  }
  
  response {
    status {
      code: 202
      text: Accepted
    }
  }
}

example {
  name: 400 Response
  description: Bad Request
  
  request {
    url: {{baseUrl}}/workouts/import/bulk
    method: POST
    mode: multipartForm
  }
  
  response {
    status {
      code: 400
      text: Bad Request
    }
  }
}
